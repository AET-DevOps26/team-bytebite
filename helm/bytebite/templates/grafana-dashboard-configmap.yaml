{{- if .Values.monitoring.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: bytebite-grafana-dashboard
  namespace: {{ .Values.namespace }}
data:
  bytebite-overview.json: |-
{{ .Files.Get "dashboards/bytebite-overview.json" | indent 4 }}
{{- end }}
