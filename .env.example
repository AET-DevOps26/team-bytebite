OPENAI_API_KEY=sk-...
LOGOS_KEY=lg-...

# Optional: only needed to use a local LM Studio model instead of a hosted provider.
# LM_STUDIO_BASE_URL=http://host.docker.internal:1234/v1
# LM_STUDIO_MODEL=your-loaded-model-identifier